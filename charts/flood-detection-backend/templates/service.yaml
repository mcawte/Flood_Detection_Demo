apiVersion: v1
kind: Service
metadata:
  name: {{ include "flood-detection-backend.fullname" . }}
  labels:
    {{- include "flood-detection-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flood-detection-backend.selectorLabels" . | nindent 4 }}